version: 1
frontend:
  phases:
    preBuild:
      commands:
        - cd app
        - npm ci
    build:
      commands:
        - npm run generate
  artifacts:
    baseDirectory: app/.output/public
    files:
      - '**/*'
  cache:
    paths:
      - app/node_modules/**/*
